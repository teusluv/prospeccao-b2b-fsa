# Perfil de desenvolvimento: banco H2 em memória, não precisa instalar nada.
spring:
  datasource:
    url: jdbc:h2:mem:prospeccao;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
