# Copie este arquivo para backend/.env e preencha. O .env NÃO vai para o GitHub.
# Use aspas simples na senha se ela tiver caracteres especiais.

SPRING_PROFILES_ACTIVE=prod

# Supabase: Connect -> Direct -> Method "Session pooler" (funciona em qualquer rede)
DB_URL='jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:5432/postgres?sslmode=require'
DB_USUARIO='postgres.gcwdqyhapnmhgpyqqllt'
DB_SENHA='coloque-a-senha-do-banco-aqui'

# Opcional: chave do Google Places (sem ela, a busca usa o OpenStreetMap)
# GOOGLE_PLACES_API_KEY='AIza...'
